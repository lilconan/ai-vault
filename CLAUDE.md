# AI 아카이브 — 작업 규칙 (Claude용)

이 폴더는 "AI 공부하면서 모으는 것들" 아카이브다.
사용자가 자료를 **막 던지면**, Claude가 **분류·요약 → 확인 → Notion + 로컬 동시 저장**한다.

---

## 🎯 핵심 워크플로우 (사용자가 뭔가 던졌을 때)

1. **분류** — Type 판단: `스킬` / `레포` / `프롬프트` / `노트`
2. **추출·생성** — 제목, 태그, 링크/출처, 상태, (프롬프트면) 모델, 그리고 본문 요약
3. **미리보기 제시** — "이렇게 넣을게요" 형식으로 Type/제목/태그/상태/요약을 먼저 보여주고 **사용자 확인을 받는다** (확인 후 저장이 원칙)
4. **OK 받으면 동시 저장**
   - **Notion**: 아래 DB에 새 항목 생성 (속성 + Type별 본문 섹션)
   - **로컬**: `<폴더>/<slug>.md` 생성 + 해당 폴더 `README.md` 목록 표에 한 줄 추가
5. **결과 보고** — Notion 항목 링크 + 로컬 파일 경로

> 트리거 예: 링크만 던짐 / "이 프롬프트 저장해줘" / "이거 아카이브에 넣어" / 스킬 설명 붙여넣기 / "이거 노트로"

---

## 🗄 Notion 저장 위치 (재사용 — 새로 만들지 말 것)

> 🔒 실제 노션 ID·워크스페이스·이메일은 **gitignore된 로컬 파일**에 보관: `.claude/notion-config.local.md` (레포 미커밋). 아래는 자리표시자.

- **워크스페이스**: `<워크스페이스명>` (`<이메일>`) — ID `<WORKSPACE_ID>`
  - ⚠️ `/mcp` Notion 연결이 이 워크스페이스여야 함. 다른 워크스페이스로 붙으면 아래 ID들이 404 → 재인증(제거 후 재연결)해서 전환.
- **부모 페이지** "AI 아카이브": `<PARENT_PAGE_ID>`
  - URL: `https://app.notion.com/p/<PARENT_PAGE_ID>`
- **DB "아카이브" (data source)**: `<DATA_SOURCE_ID>`
  - 항목 생성 시 parent: `{"data_source_id": "<DATA_SOURCE_ID>"}`
  - DB URL: `https://app.notion.com/p/<DB_ID>`

> 이 ID가 살아있으면 **절대 새 페이지/DB를 만들지 말고** 기존 DB에 append 한다.
> ID가 죽었거나 못 찾으면 `notion-search`로 "AI 아카이브" 먼저 검색, 그래도 없을 때만 재생성 후 이 파일의 ID 갱신.

### DB 속성 스키마
| 속성 | 타입 | 값 |
|------|------|-----|
| 제목 | Title | 항목 이름 |
| Type | Select | `스킬` `레포` `프롬프트` `노트` |
| 상태 | Select | `발견` `보는중` `테스트함` `실전투입` `참고용` |
| 태그 | Multi-select | claude / agent / rag / cli / mcp / prompt … (새 값 자유 추가) |
| 링크 | URL | 원본/출처 (내장·자작이면 비움) |
| 모델 | Text | 프롬프트용 모델명 (없으면 비움) |
| 추가일 | Created time | 자동 |

항목 생성은 `notion-create-pages` + `parent.data_source_id` 사용.
properties 예: `{"제목":"...", "Type":"레포", "상태":"발견", "태그":["claude","cli"], "링크":"https://..."}`
(모델 없으면 키 생략, 링크 없으면 생략)

---

## 📝 Type별 Notion 본문 템플릿 (content, Notion 마크다운)

**스킬**
```
## 뭐하는 스킬
(한 줄)
## 언제 쓰나
(트리거 상황)
## 써본 후기 / 메모
```
**레포**
```
## 뭐하는 레포
## 왜 저장했나 / 어디 쓸까
## 메모
```
**프롬프트**
```
## 프롬프트
​```
(본문)
​```
## 결과 / 메모
```
**노트**
```
(자유 본문 — 주제 정리)
```

---

## 💾 로컬 미러 규칙

- 폴더 매핑: `스킬→skills/` · `레포→repos/` · `프롬프트→prompts/` · `노트→notes/`
- 파일: `<폴더>/<slug>.md` — slug은 제목 기반 kebab-case (한글이면 영문 요약 slug 또는 원제).
- 파일 상단 헤더:
  ```
  > 태그: #claude #cli
  > 출처: https://...
  > 상태: 발견
  ```
  그 아래 위 Type별 템플릿과 같은 본문.
- 저장 후 그 폴더 `README.md`의 "목록" 표에 한 줄 추가 (기존 표 형식 그대로).

---

## ⚠️ 원칙
- **확인 전 저장 금지.** 항상 미리보기 → 승인 → 저장.
- Notion과 로컬은 **같은 내용**으로 동시에. 하나만 저장하고 끝내지 말 것.
- 없는 정보(링크·모델 등)는 지어내지 말고 비움.
- 사용자가 "그냥 바로 저장해" 라고 하면 그때만 확인 생략.
