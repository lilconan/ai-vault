# AI 아카이브 — 작업 규칙 (Claude용)

이 폴더는 "AI 공부하면서 모으는 것들" 아카이브다.
사용자가 자료를 **막 던지면**, Claude가 **분류·요약 → 확인 → Notion + 로컬 동시 저장**한다.

## 📎 참조 파일
- **노션 ID·워크스페이스·이메일**: `.claude/notion-ids.md` (gitignore, 미커밋). 없으면 `.claude/notion-ids.example.md`를 복사해 채운다.
- **노션 DB 스키마·본문 템플릿·신선도 수식**: `.claude/rules/notion-schema.md`
- **리서치 문서 정리 커맨드**: `.claude/commands/노션정리.md` (`/노션정리 <파일>`)

---

## 🎯 핵심 워크플로우 (사용자가 뭔가 던졌을 때)

1. **분류** — Type 판단: `스킬` / `레포` / `프롬프트` / `노트`
2. **추출·생성** — 제목, 태그, 링크/출처, 상태, (프롬프트면) 모델, 본문 요약. 리서치·도구 문서면 리서치 날짜·부패속도도 채운다.
3. **미리보기 제시** — "이렇게 넣을게요" 형식으로 Type/제목/태그/상태/요약을 먼저 보여주고 **사용자 확인을 받는다** (확인 후 저장이 원칙).
4. **OK 받으면 동시 저장**
   - **Notion**: 아카이브 DB에 새 항목 (속성 + Type별 본문). 스키마는 `notion-schema.md`, DB ID는 `notion-ids.md`.
   - **로컬**: `<폴더>/<slug>.md` 생성 + 해당 폴더 `README.md` 목록 표에 한 줄 추가.
5. **결과 보고** — Notion 항목 링크 + 로컬 파일 경로.

> 트리거 예: 링크만 던짐 / "이 프롬프트 저장해줘" / "이거 아카이브에 넣어" / 스킬 설명 붙여넣기 / "이거 노트로"

---

## 🗄 Notion 구조 (요약)
- "AI 아카이브" 페이지 밑에 **아카이브 DB**(모든 항목, Type 구분) + **도구·레시피 DB**(설치물) 2개.
- **재사용 원칙**: ID가 살아있으면 새 DB/페이지 만들지 말고 기존 DB에 append. (자세한 스키마·수식·생성법은 `.claude/rules/notion-schema.md`)
- ⚠️ `/mcp` Notion이 `notion-ids.md`의 워크스페이스에 붙어 있어야 함. 다른 워크스페이스면 ID가 404 → 재인증(제거 후 재연결)으로 전환.

---

## 💾 로컬 미러 규칙

- 폴더 매핑: `스킬→skills/` · `레포→repos/` · `프롬프트→prompts/` · `노트→notes/`
- 파일: `<폴더>/<slug>.md` — slug은 제목 기반 kebab-case (한글이면 영문 요약 slug 또는 원제).
- 파일 상단 헤더:
  ```
  > 태그: #claude #cli
  > 출처: https://...
  > 상태: 발견
  ```
  그 아래 Type별 템플릿(`notion-schema.md`)과 같은 본문.
- 저장 후 그 폴더 `README.md`의 "목록" 표에 한 줄 추가 (기존 표 형식 그대로).

---

## ⚠️ 원칙
- **확인 전 저장 금지.** 항상 미리보기 → 승인 → 저장.
- Notion과 로컬은 **같은 내용**으로 동시에. 하나만 저장하고 끝내지 말 것.
- 없는 정보(링크·모델·날짜 등)는 지어내지 말고 비움.
- 실제 노션 ID·이메일은 추적 파일에 쓰지 말 것 — `.claude/notion-ids.md`(gitignore)에만.
- 사용자가 "그냥 바로 저장해" 라고 하면 그때만 확인 생략.
